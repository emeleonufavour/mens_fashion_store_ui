class Constants {
  static const double initAnimationOffset = 100;
  static const double bodyHorizontalPadding = 16;

  static const double modelWidth = 500;

  static const double scaleFraction = 0.08;
  static const double xOffset = -50;

  static const Duration backgroundModelsAnimationDuration =
      Duration(milliseconds: 300);
  static const Duration swipeAnimationDuration = Duration(milliseconds: 500);
}
